# Working with LLMs

The Prompt Manager can launch different LLMs in the system's default browser via configurable buttons. Add or modify LLM URLs in the configuration file located at `~/.promptmgr.json`.

This documentation is intended for language models to understand the application's features. The use of any particular LLM or AI coding tool is optional.
